name: Manual Workflow

on:
  repository_dispatch:
    types:
      - event_type

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Delpoy manual
        run: echo "Deployment workflow manually with API Call"
